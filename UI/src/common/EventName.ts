class EventName {
    // Mở form chi tiết thực đơn
    static ShowFormMenuDetail = 'ShowFormMenuDetail';

    // Tạo toast message
    static ShowToastMessage = 'ShowToastMessage';

    // Quay về trang login
    static RedirectToLogin = 'RedirectToLogin';

    // Mở form chi tiết đặt bàn
    static ShowFormReservationDetail = 'ShowFormReservationDetail';

    // Mở form chi tiết đặt bàn
    static ShowFormCustomerDetail = 'ShowFormCustomerDetail';

    // Mở dialog thông báo
    static ShowDialog = 'ShowDialog';

    // Mở form chi tiết nhân viên
    static ShowFormInventoryItemDetail = 'ShowFormInventoryItemDetail';

    // Mở form chi tiết nhân viên
    static ShowFormEmployeeDetail = 'ShowFormEmployeeDetail';
};

export default EventName;